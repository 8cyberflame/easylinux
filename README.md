# easylinux
EasyLinux is a simple Python-based terminal tool for Debian/Ubuntu that makes Linux easier to use. It lets you install/remove packages, update the system (APT + Flatpak), and run basic system commands through a clean command-line interface.
